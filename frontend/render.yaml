services:
  - type: static
    name: trigonaut-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://your-backend-url
    routes:
      - path: /*
        destination: /index.html
        matchType: rewrite
